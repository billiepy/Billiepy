name: WakaTime status update

on:
  schedule:
    - cron: "1 0 * * *"
  workflow_dispatch:

jobs:
  update-readme:
    name: Update the WakaTime Stat
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Update README with WakaTime stats
        uses: avinal/Profile-Readme-WakaTime@master
        env:
          GIT_AUTHOR_NAME: billiepy
          GIT_AUTHOR_EMAIL: arclx724@gmail.com
          GIT_COMMITTER_NAME: billiepy
          GIT_COMMITTER_EMAIL: arclx724@gmail.com
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GITHUB_TOKEN: ${{ github.token }}
          BRANCH: "main" # Yahan maine 'master' ko 'main' kar diya hai
          COMMIT_MSG: "chore(readme): auto-update WakaTime stats"
          STATS_RANGE: "last_7_days"
          
